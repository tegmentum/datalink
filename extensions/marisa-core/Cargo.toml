[package]
name = "marisa-core"
version = "0.1.0"
edition = "2021"
description = "Neutral core for the marisa extension (fst trie set lookup/prefix): logic + capability declaration, written ONCE."
license = "MIT"
repository = "https://github.com/tegmentum/datalink"

[lib]
name = "marisa_core"
path = "src/lib.rs"

[dependencies]
datalink-extcore = { path = "../../crates/datalink-extcore" }
fst = "0.4"
serde_json = "1"
