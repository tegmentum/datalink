[package]
name = "csscolor-core"
version = "0.1.0"
edition = "2021"
description = "Neutral, DB-agnostic core for the `csscolor` extension (CSS colour parsing + conversion via csscolorparser): the logic + the capability declaration, written ONCE. The per-DB shims are generated from this declaration by datalink-extcore."
license = "MIT"
repository = "https://github.com/tegmentum/datalink"

[lib]
name = "csscolor_core"
path = "src/lib.rs"

[dependencies]
datalink-extcore = { path = "../../crates/datalink-extcore" }
csscolorparser = "0.8"
