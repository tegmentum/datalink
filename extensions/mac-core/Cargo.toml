[package]
name = "mac-core"
version = "0.1.0"
edition = "2021"
description = "Neutral core for the mac extension (mac_valid / mac_normalize): logic + capability declaration, written ONCE."
license = "MIT"
repository = "https://github.com/tegmentum/datalink"

[lib]
name = "mac_core"
path = "src/lib.rs"

[dependencies]
datalink-extcore = { path = "../../crates/datalink-extcore" }
macaddr = "1"
